DB_HOST=localhost
DB_PORT=5432
DB_USER=app_user
DB_PASSWORD=app_password
DB_NAME=hc_dev
PORT=3000

# Set to true to enable the legacy GraphQL Playground UI at /graphql.
# Apollo Sandbox is available in development by default when this is unset.
GRAPHQL_PLAYGROUND=false
